# service definition
---
kind: Service
apiVersion: v1
metadata:
  name: lighthouse
  namespace: default
spec:
  selector:
    app: lighthouse
  type: ClusterIP
  ports:
  - port: 15000
    targetPort: 15000
    name: lighthouse-15000
